; Keep this file portable. Docker installs and enables pdo_mysql in the image.
; If you run PHP directly on Windows, enable pdo_mysql in that PHP installation's
; main php.ini instead of hardcoding a machine-specific extension_dir here.
